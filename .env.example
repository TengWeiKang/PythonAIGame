# =============================================================================
# PYTHON GAME DETECTION SYSTEM - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and configure your settings
# IMPORTANT: Remove .example from the filename (save as just ".env")

# =============================================================================
# GOOGLE GEMINI API CONFIGURATION (REQUIRED FOR CHATBOT)
# =============================================================================

# Your Gemini API key - Get it from: https://makersuite.google.com/app/apikey
# Format: Must start with "AIza" followed by 35 characters (letters, numbers, - or _)
# Example: AIzaSyDemoKeyForExampleOnly123456789abcde
GEMINI_API_KEY=your_gemini_api_key_here

# =============================================================================
# OPTIONAL: ADVANCED AI SETTINGS
# =============================================================================

# Gemini model to use (recommended: gemini-1.5-flash for speed)
# Options: gemini-1.5-flash, gemini-1.5-pro, gemini-pro, gemini-pro-vision
GEMINI_MODEL=gemini-1.5-flash

# Request timeout in seconds (5-300, default: 30)
GEMINI_TIMEOUT=30

# Generation temperature (0.0-1.0, lower = more focused, higher = more creative)
GEMINI_TEMPERATURE=0.7

# Maximum response tokens (1-8192, default: 2048)
GEMINI_MAX_TOKENS=2048

# Enable rate limiting to prevent quota exhaustion (recommended: true)
GEMINI_RATE_LIMITING=true

# Maximum requests per minute (1-100, default: 15)
GEMINI_REQUESTS_PER_MINUTE=15

# =============================================================================
# OPTIONAL: APPLICATION SETTINGS
# =============================================================================

# Enable debug logging for troubleshooting (default: false)
DEBUG_LOGGING=false

# Custom directory paths (leave empty or comment out to use defaults)
# DATA_DIR=data
# MODELS_DIR=data/models
# RESULTS_EXPORT_DIR=data/results

# =============================================================================
# QUICK SETUP GUIDE
# =============================================================================
#
# 1. COPY FILE: Save this file as ".env" (remove ".example")
# 2. GET API KEY: Visit https://makersuite.google.com/app/apikey
# 3. REPLACE KEY: Change "your_gemini_api_key_here" to your actual API key
# 4. SAVE & RESTART: Save the file and restart the application
#
# =============================================================================
# TROUBLESHOOTING
# =============================================================================
#
# If chatbot shows "not configured":
# 1. Run: python diagnose_chatbot_config.py
# 2. Check that .env file exists in the project directory
# 3. Verify API key format (starts with AIza, 39 characters total)
# 4. Ensure no extra spaces or quotes around the API key
# 5. Restart the application after making changes
#
# Common Issues:
# - File named ".env.example" instead of ".env"
# - API key in wrong format or contains errors
# - File permissions preventing reading
# - API key quota exceeded or invalid
#
# =============================================================================